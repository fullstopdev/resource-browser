name: v1
schema:
  openAPIV3Schema:
    description: BridgeDomainState is the Schema for the bridgedomainstates API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: BridgeDomainStateSpec defines the desired state of BridgeDomainState
        properties:
          evi:
            description: EVI in use for this BridgeDomain
            maximum: 65535
            minimum: 1
            type: integer
          exportTarget:
            description: Export route target for this BridgeDomain, in the format 'target:N:N'
            pattern: ^target.*$
            type: string
          importTarget:
            description: Import route target for this BridgeDomain, in the format 'target:N:N'
            pattern: ^target.*$
            type: string
          tunnelIndex:
            description: Vxlan tunnel index in use
            type: integer
          vni:
            description: VNI in use for this BridgeDomain
            maximum: 16777215
            minimum: 1
            type: integer
        type: object
      status:
        description: BridgeDomainStateStatus defines the observed state of BridgeDomainState
        type: object
    type: object
served: true
storage: true
subresources:
  status: {}
