name: v1
schema:
  openAPIV3Schema:
    description: OSPFInstanceState is the Schema for the ospfinstancestates API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: OSPFInstanceStateSpec defines the desired state of OSPFInstanceState
        properties:
          addressFamily:
            description: Address family for OSPFv3
            type: string
          defaultOSPFInstance:
            description: Denotes if the group is a DefaultOSPFInstance or OSPFInstance
            type: boolean
          enabled:
            description: Indicates whether the OSPF Insance is administratively enabled
            type: boolean
          version:
            description: OSPF version used
            type: string
        required:
          - defaultOSPFInstance
          - enabled
        type: object
      status:
        description: OSPFInstanceStateStatus defines the observed state of OSPFInstanceState
        type: object
    type: object
served: true
storage: true
subresources:
  status: {}
