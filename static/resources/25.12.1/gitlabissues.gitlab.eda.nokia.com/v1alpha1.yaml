name: v1alpha1
schema:
  openAPIV3Schema:
    description: GitlabIssue is the Schema for the gitlabissues API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: spec defines the desired state of GitlabIssue
        properties:
          closeOnResolve:
            description: Close the issue when resolved.
            type: boolean
          instance:
            description: Gitlab instance name.
            type: string
          issue:
            properties:
              assignees:
                description: Assignees of the issue.
                items:
                  type: string
                type: array
              body:
                description: Body of the issue.
                type: string
              labels:
                description: Labels of the issue.
                items:
                  type: string
                type: array
              milestone:
                description: Milestone of the issue.
                type: string
              title:
                description: Title of the issue.
                type: string
            type: object
          repo:
            type: string
          trigger:
            properties:
              alarm:
                properties:
                  exclude:
                    description: Excluded alarm types that don't trigger the workflow
                    items:
                      type: string
                    type: array
                  include:
                    description: Included alarm types that trigger the workflow
                    items:
                      type: string
                    type: array
                type: object
              query:
                properties:
                  fields:
                    description: List of fields to watch
                    items:
                      type: string
                    type: array
                  path:
                    description: Path to the EDB table to watch
                    type: string
                  where:
                    description: Where clause to filter the results
                    type: string
                type: object
            type: object
        type: object
      status:
        description: status defines the observed state of GitlabIssue
        type: object
    required:
      - spec
    type: object
served: true
storage: true
subresources:
  status: {}
