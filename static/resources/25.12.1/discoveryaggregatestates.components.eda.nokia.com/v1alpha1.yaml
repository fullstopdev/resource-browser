name: v1alpha1
schema:
  openAPIV3Schema:
    description: DiscoveryAggregateState is the Schema for the discoveryaggregatestates API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: DiscoveryAggregateStateSpec defines the desired state of DiscoveryAggregateState
        properties:
          nodes:
            description: List of TopoNodes this discovery is for.
            items:
              type: string
            type: array
        type: object
      status:
        description: DiscoveryAggregateStateStatus defines the observed state of DiscoveryAggregateState
        type: object
    type: object
served: true
storage: false
subresources:
  status: {}
