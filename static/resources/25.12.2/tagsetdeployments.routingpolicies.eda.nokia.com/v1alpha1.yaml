name: v1alpha1
schema:
  openAPIV3Schema:
    description: TagSetDeployment is the Schema for the tagsetdeployments API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: TagSetDeploymentSpec defines the desired state of TagSetDeployment
        properties:
          node:
            description: Specifies a Node to deploy this Set on
            type: string
          tagSet:
            description: Specifies a TagSet to deploy to the specified Node
            type: string
        required:
          - node
        type: object
      status:
        description: TagSetDeploymentStatus defines the observed state of TagSetDeployment
        type: object
    type: object
served: true
storage: true
subresources:
  status: {}
