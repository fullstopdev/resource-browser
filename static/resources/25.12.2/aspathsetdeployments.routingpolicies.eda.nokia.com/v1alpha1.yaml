name: v1alpha1
schema:
  openAPIV3Schema:
    description: ASPathSetDeployment is the Schema for the aspathsetdeployments API
    properties:
      apiVersion:
        description: |-
          APIVersion defines the versioned schema of this representation of an object.
          Servers should convert recognized schemas to the latest internal value, and
          may reject unrecognized values.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        type: string
      kind:
        description: |-
          Kind is a string value representing the REST resource this object represents.
          Servers may infer this from the endpoint the client submits requests to.
          Cannot be updated.
          In CamelCase.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      metadata:
        type: object
      spec:
        description: ASPathSetDeploymentSpec defines the desired state of ASPathSetDeployment
        properties:
          asPathSet:
            description: Specifies an ASPathSet to deploy to the specified Node
            type: string
          node:
            description: Specifies a Node to deploy this policy on
            type: string
        required:
          - node
        type: object
      status:
        description: ASPathSetDeploymentStatus defines the observed state of ASPathSetDeployment
        type: object
    type: object
served: true
storage: true
subresources:
  status: {}
