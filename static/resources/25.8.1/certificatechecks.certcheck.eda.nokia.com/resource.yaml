apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: "2025-08-16T12:04:32Z"
  generation: 1
  name: certificatechecks.certcheck.eda.nokia.com
  resourceVersion: "2659"
  uid: 63239cd1-562b-4de6-b306-c572ec4d22be
spec:
  conversion:
    strategy: None
  group: certcheck.eda.nokia.com
  names:
    kind: CertificateCheck
    listKind: CertificateCheckList
    plural: certificatechecks
    singular: certificatecheck
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CertificateCheck is the Schema for the certificatechecks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateCheckSpec defines the desired state of CertificateCheck
            properties:
              address:
                description: list of addresses to check
                items:
                  type: string
                type: array
              interval:
                default: 5m
                description: Interval specifies the check interval
                type: string
              podSelector:
                description: PodSelector defines a selector for the target Pods to
                  check.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              service:
                description: Service defines the name of the Kubernetes Service to
                  check.
                items:
                  type: string
                type: array
              serviceSelector:
                description: ServiceSelector defines a label selector for dynamically
                  selecting the service
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              targetNode:
                description: list of Targetnodes to check
                items:
                  type: string
                type: array
              targetNodeSelector:
                description: TargetNodeSelector defines a label selector for dynamically
                  selecting targetNodes
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              thresholds:
                description: Thresholds defines the certificate validity thresholds
                  for raising alarms
                properties:
                  critical:
                    description: Critical threshold in percentage
                    maximum: 100
                    minimum: 0
                    type: integer
                  major:
                    description: Major threshold in percentage
                    maximum: 100
                    minimum: 0
                    type: integer
                  minor:
                    description: Minor threshold in percentage
                    maximum: 100
                    minimum: 0
                    type: integer
                  warning:
                    description: Warning threshold in percentage
                    maximum: 100
                    minimum: 0
                    type: integer
                type: object
              timeout:
                default: 5s
                description: Timeout specifies the tls timeout
                type: string
              trustBundle:
                description: |-
                  TrustBundle is a ConfigMap with a `trust-bundle.pem` key containing
                  certificate authorities to be used to verify the returned certificates.
                type: string
            type: object
          status:
            description: CertificateCheckStatus defines the observed state of CertificateCheck
            properties:
              endpointStatuses:
                description: EndpointStatuses contains the certificate statuses for
                  each target endpoint
                items:
                  description: EndpointStatus defines the status of the certificate
                    for a specific endpoint
                  properties:
                    address:
                      description: Address is the address of the endpoint (service
                        or pod)
                      type: string
                    error:
                      description: Error provides any error message that occurred
                        during the check
                      type: string
                    expiryDate:
                      description: ExpiryDate is the expiration date of the certificate
                      format: date-time
                      type: string
                    issueDate:
                      description: IssueDate is the date when certificate was issued
                      format: date-time
                      type: string
                    podName:
                      type: string
                    serviceName:
                      type: string
                    targetNodeName:
                      type: string
                    valid:
                      description: Valid indicates whether the certificate is valid
                        or not
                      type: boolean
                  required:
                  - address
                  - valid
                  type: object
                type: array
              lastChecked:
                description: LastChecked indicates the last time the certificate was
                  checked
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: CertificateCheck
    listKind: CertificateCheckList
    plural: certificatechecks
    singular: certificatecheck
  conditions:
  - lastTransitionTime: "2025-08-16T12:04:32Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2025-08-16T12:04:32Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
